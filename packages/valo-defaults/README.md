# valo-defaults

`valo-defaults` will contain Valo-owned defaults and migrations for installed
systems.

In `v0.1`, it only installs the migration directory and a version marker. This
keeps the package harmless while proving the update path.
